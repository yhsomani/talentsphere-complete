import { BillingPage } from '@/features/settings/BillingPage';

export const metadata = {
  title: 'Billing & Plans | TalentSphere',
  description: 'Manage your subscription and billing details',
};

export default function Page() {
  return <BillingPage />;
}
